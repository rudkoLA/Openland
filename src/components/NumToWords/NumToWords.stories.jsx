import React from 'react';
import { storiesOf } from '@storybook/react';

import { NumToWords } from './NumToWords';

storiesOf('NumToWords', module).add('default', () => (
	<NumToWords> </NumToWords>
));
